points=input("please enter all the point by separeting by space\n\t").split()
#for n in range( 0,len(points)):
    #points[n]=int(points[n])
highest_score=0
for point in points:
    int(points[point])
    if point>highest_score:
        highest_score=point
print(highest_score)
        
        
        
        